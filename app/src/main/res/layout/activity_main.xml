<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:background="#000"
        android:layout_height="match_parent"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        tools:context="phamhaidang.itplus.vn.truyenapp.MainActivity">
    <FrameLayout android:layout_width="match_parent"
                  android:layout_height="wrap_content"
                  android:id="@+id/maintoptab"
                  android:gravity="center"
                  android:padding="5dp"
                  android:orientation="horizontal"
                  android:layout_alignParentTop="true">
    </FrameLayout>

    <android.support.v4.view.ViewPager android:layout_width="match_parent"
                                       android:layout_height="match_parent"
                                       android:layout_below="@+id/maintoptab"
                                       android:layout_above="@+id/mainmnbottom"
                                       android:id="@+id/mainpager"
    />

    <android.support.design.widget.TabLayout android:layout_width="match_parent"
                                             android:layout_height="60dp"
                                             app:tabSelectedTextColor="@color/menutext"
                                             app:tabTextAppearance="@style/TabStyle"
                                             android:layout_alignParentBottom="true"
                                             android:id="@+id/mainmnbottom"/>
</RelativeLayout>
